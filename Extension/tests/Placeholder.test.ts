// Placeholder test for extension
import { describe, it, expect } from 'vitest';

describe('Extension initialization', () => {
  it('should have proper setup', () => {
    expect(true).toBe(true);
  });
});
